name: Custom Interaction 
on:
  issues:
    types: [opened]
  pull_request_target:
    types: [opened]

jobs:

  # test action works running from the graph
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - uses: ./
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        issue: |
          Hello there what are doing